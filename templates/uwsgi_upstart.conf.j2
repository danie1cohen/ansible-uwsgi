# uWSGI upstart script

description "UWSGI startup file for {{ uwsgi_project }}"

start on runlevel [2345]
stop on runlevel [06]

respawn

exec {{ uwsgi_exec_path }} --ini {{ uwsgi_install_path }}/{{ uwsgi_project }}_uwsgi.ini
